# Given its dimension (lado),           #
# calculates equilateral triangle area  #
# (resultado)                           #
program

    # Declaration section #
    lado;
    resultado;

begin

    # Command section #
    lado = $F;
    resultado = lado^$2/2;
    
end
